name: Run Shared Terraform Workflow

on:
  push:
    branches: [main]
permissions:
  id-token: write
  contents: read

jobs:
  terraform:
    uses: natalia-castaneda/central-CI/.github/workflows/terraform.yml@main

